{
    "fields": [
        {
            "name": "title",
            "type": "string"
        }
    ],
    "validations": [
        {
            "field": "title",
            "rules": "required"
        }
    ]
}